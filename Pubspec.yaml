name: betpredix
description: Fantasy Sports Predictions App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  firebase_core: ^2.24.0
  firebase_auth: ^4.14.0
  cloud_firestore: ^4.14.0
  firebase_messaging: ^14.7.0
  
  provider: ^6.0.0
  
  http: ^1.1.0
  
  intl: ^0.19.0
  
  flutter_local_notifications: ^16.0.0
  
  shared_preferences: ^2.2.0
  
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
